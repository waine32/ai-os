# AI-OS

Bash CLI router for DeepSeek API. No frameworks, just `curl` + `jq`.
Key files: `ai` (main script), `tests/ai.bats`, `CLAUDE.md`, `ds.md`.
Run tests: `bats tests/ai.bats`
Syntax check: `bash -n ai`
